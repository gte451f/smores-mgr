import ValidatedInput from '../validated-input/component';

/**
 * extend form validated-input
 */
export default ValidatedInput.extend({

  // info tooltip bubble, just for text inputs
  tooltip: false,
});
