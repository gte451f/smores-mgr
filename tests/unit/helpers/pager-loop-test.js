import {
  pagerLoop
} from '../../../helpers/pager-loop';
import { module, test } from 'qunit';

module('PagerLoopHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = pagerLoop(42);
  assert.ok(result);
});
